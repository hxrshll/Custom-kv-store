{
  "name": "custom-kv-store",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "dev": "ts-node src/index.ts",
    "server": "ts-node src/server/tcp-server.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "ts-node": "^10.0.0",
    "@types/node": "^20.0.0"
  }
}
